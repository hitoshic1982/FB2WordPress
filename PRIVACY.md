# Privacy / 隱私 / 隐私 / プライバシー

## 繁體中文

FB2WordPress 是本機桌面程式，不使用炎劍文化工作室的雲端中繼服務。程式會讀取你選擇的 Facebook ZIP，並依你的操作直接連線你的 WordPress REST API；只有選擇上傳影片時才會連線 Google OAuth 與 YouTube API。網站設定、應用程式密碼、OAuth 資料與更新權杖保存在目前 Windows 使用者的 `%LocalAppData%\FB2WordPress`，敏感設定以 Windows DPAPI 加密；移轉進度也存於同一資料夾。

## 简体中文

FB2WordPress 是本地桌面程序，不使用炎剑文化工作室的云端中转服务。程序读取你选择的 Facebook ZIP，并直接连接你的网站 REST API；只有上传视频时才连接 Google OAuth 和 YouTube API。网站设置、应用程序密码、OAuth 数据和刷新令牌保存在当前 Windows 用户的 `%LocalAppData%\FB2WordPress`，敏感设置使用 Windows DPAPI 加密。

## English

FB2WordPress is a local desktop app and uses no Flameblade Studio relay service. It reads only the Facebook ZIP you select and connects directly to your WordPress REST API. Google OAuth and YouTube are contacted only when you choose to upload videos. Site settings, the Application Password, OAuth data, and refresh tokens are stored under `%LocalAppData%\FB2WordPress`; sensitive settings are protected with Windows DPAPI.

## 日本語

FB2WordPress はローカルデスクトップアプリで、Flameblade Studio の中継クラウドを使用しません。選択した Facebook ZIP を読み、利用者の WordPress REST API に直接接続します。Google OAuth と YouTube API は動画をアップロードする場合だけ使用します。サイト設定、アプリケーションパスワード、OAuth 情報、更新トークンは `%LocalAppData%\FB2WordPress` に保存し、機密設定を Windows DPAPI で保護します。

